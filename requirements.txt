# Core runtime packages
python-dateutil>=2.8.0
requests>=2.25.0

# System monitoring for Lambda optimization
psutil>=5.9.8

# Built-in modules used (no installation required):
# re, html, urllib.parse, concurrent.futures
# typing, logging, time, os, json
